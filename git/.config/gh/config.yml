git_protocol: https
editor:
prompt: enabled
pager:
aliases:
    co: pr checkout
    personal: '!cp ~/.config/gh/hosts.yml.personal ~/.config/gh/hosts.yml && gh auth setup-git && git config --global user.name "arevalolance" && git config --global user.email "hi@arevalolance.com" && gh auth status'
    work: '!cp ~/.config/gh/hosts.yml.work ~/.config/gh/hosts.yml && gh auth setup-git && git config --global user.name "lancearevalo-fgi" && git config --global user.email "lance.arevalo@focusglobalinc.com" && gh auth status'
http_unix_socket:
browser:
version: "1"
